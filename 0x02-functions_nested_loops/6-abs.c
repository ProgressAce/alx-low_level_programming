#include <stdlib.h>
#include "main.h"

/* function prototype for abs */
int abs(int n);

/**
 * _abs - prints the absolute value of a number
 * @n: the number whose absolute value is to be printed
 *
 * Return: always 0
 */

int _abs(int n)
{
	return (abs(n));
}
