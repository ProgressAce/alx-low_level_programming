#include <stdlib.h>

/**
 * _islower -  Checks for lowercase character.
 * @c: the character to be checked
 *
 * Return: always 0
 */

int _islower(int c)
{
	if (_islower(c))
		return (1);
	else
		return (0);
}
